import { MouseDirectiveDirective } from './mouse-directive.directive';

describe('MouseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
